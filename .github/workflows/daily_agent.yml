name: Agente Tesis - Batch 24h

on:
  workflow_dispatch:
  schedule:
    - cron: '0 7 * * *'

permissions:
  contents: write

jobs:
  scraping_batch:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo "Workflow funcionando"
