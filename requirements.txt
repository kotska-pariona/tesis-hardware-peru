# ──────────────────────────────────────────────────────────
# requirements.txt  — Pipeline de scraping (daily_agent.yml)
# Instalar: pip install -r requirements.txt
# ──────────────────────────────────────────────────────────

# Core HTTP
requests>=2.31.0
urllib3>=2.0.0

# HTML parsing
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Config
python-dotenv>=1.0.0

# Data
pandas>=2.1.0
# FIX: upper bound para compatibilidad con tensorflow en requirements-ml.txt
numpy>=1.26.0,<2.0

# FIX: kaggle faltaba — requerido por scraper_kaggle.py
kaggle>=1.6.0
